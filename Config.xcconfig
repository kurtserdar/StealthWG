// Shared build configuration (committed).
//
// Personal signing values (e.g. DEVELOPMENT_TEAM) live in Local.xcconfig,
// which is gitignored. The optional include below pulls them in when present
// and is silently skipped on a fresh clone that has no Local.xcconfig yet.
#include? "Local.xcconfig"
